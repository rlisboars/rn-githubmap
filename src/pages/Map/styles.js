import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  calloutContainer: {
    backgroundColor: "#FFF",
    color: "#000",
    fontSize: 12,
    width: 220,
    padding: 10
  },
  calloutTitle: {
    fontWeight: "bold",
    marginBottom: 5
  },
  pointerImage: {
    borderColor: "#FFF",
    borderRadius: 40,
    borderWidth: 4,
    height: 80,
    width: 80
  }
});

export default styles;
